#include <bits/stdc++.h>
using namespace std;
#define dbg(x) cerr<<#x<<" = "<<x<<endl


class Solution {
public:
    int characterReplacement(string s, int k) {
        /**
         * * ideas
         * * try to replace characters to a, b, c ... z
         * * for(char c: ab...z) for(char ch: s) O(n * 26)
         * * 
        */
    }
};


signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    #ifndef ONLINE_JUDGE 
        freopen("input.txt","r",stdin);
        freopen("output.txt","w",stdout);
        freopen("error.txt", "w", stderr);
    #endif
 
    return 0;
}